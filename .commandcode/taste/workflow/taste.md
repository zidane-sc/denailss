# Workflow

- Poses hypothetical product/design questions ("what if we apply login required in booking flow?") to evaluate a design alternative before committing to it — expects the assistant to first verify the current implementation (components, services, API/auth plumbing) and then deliver an honest feasibility assessment including user-experience/business tradeoffs (e.g., forcing login on an impulse-purchase flow = conversion tax; guest + claim-on-login keeps the funnel open) that ends with a concrete recommended direction and an implementation offer, not just a verdict. Confidence: 0.5

- Wants explicit consent before consequential browser actions: pause and ask rather than acting (e.g., only click a GitHub star "if I explicitly say yes"; pause and ask before any task that needs login or confirmation). Confidence: 0.9

- Prefers consequential logout actions to require an in-app confirmation modal before the session is cleared; the modal should provide clear "Batal" and "Keluar" actions and only submit logout after explicit confirmation. Confidence: 0.85

- Wants tooling set up to be verified end-to-end after install/update ("install or update ... then verify it works") — not just installed and reported as done. Confidence: 0.6

- When a flow was previously shipped as a mock/placeholder (e.g., a contact form whose submit only showed a success toast after a `setTimeout`), a terse "implement the real flow!" means replace it end-to-end with real persistence — DB table + hand-authored RLS migration, a public rate-limited API route, form rewiring to the API, and an owner-facing backoffice inbox (list, unread badge, mark-read, WhatsApp reply) — following the codebase's existing patterns (service + zod schema + DTO, owner-gated routes, response/error helpers) and finishing with typecheck/lint/tests plus docs/PROGRESS.md updates, rather than a lighter shortcut. Confidence: 0.6

- Prefers the assistant to read the official skill/setup documentation first before executing a task ("Read the BrowserAct skill first"). Confidence: 0.5

- Wants the assistant to commit completed work to git as part of wrapping up a task ("commit current changes") rather than leaving the repo with uncommitted changes; expects the assistant to handle staging and committing, including pre-existing uncommitted work from others. Also, when offered options after a session wraps (commit vs. starting the backend phase vs. a lint hardening pass), prioritizes committing the working tree before anything else — replied tersely "comit first". Fresh confirmation: a bare "okay commit then push" right after a task wrapped was executed as a scoped commit + push to origin/main, with the result reported and accepted without further request. Confidence: 0.85

- Wants project documentation kept in sync with code changes — "update @docs if needed" (e.g., AGENTS.md, docs/PROGRESS.md) should be checked and updated as part of finishing a task; proactively asks whether docs need updating for the latest changes ("do we need update @docs about our changes?") and also asks the assistant to audit that the docs match the actual implementation, not just that they were updated (e.g., "apa sudah sesuai antara docs dan implementasi?"). Expects the doc to mirror the final shipped state of "our changes" broadly — including internal architecture refactors (e.g., a seed/store split for SSR safety) and intentional design decisions (e.g., a deliberately omitted delete feature) — not just user-visible feature lists. Reiterated explicitly right after an epic shipped ("okay then update the @docs"): a blanket docs request that expects the assistant to determine which docs need updating and to record the shipped technical decisions in the spec too (added settings to TRD entities/ERD, a data-model subsection, API endpoints, and a new ADR), not just the progress log. Confidence: 0.99

- Commits authored by the assistant are expected to follow conventional-commit format with a scoped `feat:`/`fix:` summary, a detailed bullet-point body, and the `Co-authored-by: CommandCodeBot` attribution line — the user hasn't objected to this format in reviews; a fresh instance (detailed `feat:` body for a 173-file commit + `chore:` for `.nvmrc`) shipped under a terse "go" approval; the "commit all then push it" catch-all commit also shipped with the same `feat:` scope summary, bullet body, and `Co-authored-by: CommandCodeBot` line, accepted without comment. Fresh confirmation: a bare "commit then push" shipped a scoped `fix:` commit (service edit modal + free-form input fixes) with a detailed bullet body and `Co-authored-by: CommandCodeBot` line, accepted without comment. Fresh confirmation: a bare "okay commit then push" shipped a scoped `feat:` commit (landing nav anchors + settings-driven maps link) with a conventional-commit summary, a detailed bullet-point body, and the `Co-authored-by: CommandCodeBot` line, accepted without comment. Confidence: 0.8

- Wants competitive/market research grounded in authoritative sources (official studio price lists, reputable articles like Kompas, market-data sites) before deciding whether the current service/feature set is complete and what to add — e.g., "check our services, is it enough? can you do research what service should we add?" Confidence: 0.7

- Prefers a propose-then-approve workflow for significant features (and notable doc/implementation additions): the assistant researches, presents a concrete plan/recommendation, and waits for explicit go-ahead before implementing — rather than jumping straight to code. Gives terse single-word approvals to greenlight execution (e.g., "yeah", "go", "iya", "continue" — approving the image-upload feature proposal and greenlighting the next roadmap epic after the assistant described it). Confirmed again with a bare "go!" greenlighting the FAQ-migration proposal (fold landing FAQ into the settings table as JSONB) after the assistant presented the two options and asked to proceed; and with a terse "go fix" approving the promo image-upload implementation right after the assistant offered it ("Want me to implement it?"); and approved fixing the just-identified `imagePath()` SEO gap with a bundled terse "run the seed and fix the gap" in response to "Want me to fix the imagePath() stub in seo.tsx while we're at it?"; and greenlit the full landing-page performance plan (ISR revalidation + settings-read hoisting + lazy-load hints) with a bare "apply it!" right after the assistant's ranked audit ended with an implementation offer — expecting the whole plan executed and verified in one pass, not just the first item. Fresh confirmation: after a full availability/appointment flow review, the assistant offered to fix both issues it had just identified (stale slots after booking creation, plus the HH:MM:SS/HH:MM format inconsistency in `getBookedSlotsByDate`) and the user replied "go fix" — both fixes were implemented and verified (typecheck + lint) in one pass with no further direction. Fresh confirmation: after the landing-page audit ended with a scoped offer ("Want me to implement those two?" — anchor nav + settings-driven mapsUrl), a bare "go!" greenlit exactly the recommended two fixes; the assistant implemented both (desktop + mobile nav anchors with URL-hash persistence, contact maps link from settings with SITE fallback), self-cleaned an unused const it had introduced, verified with typecheck + lint, and marked its todo list complete — accepted without objection. Confidence: 0.99

- Overrides the assistant's reasoned recommendation with terse, decisive instructions when they already know what they want — e.g., after the assistant recommended keeping "related services" because public pages use it, the user simply said "hapus 2-2nya, hapus juga yang existing" (delete both, including existing data) and expected the full removal executed without further debate. Confidence: 0.7

- Redirects already-shipped functionality with a single terse sentence when the implementation doesn't match their operational reality — right after a full CRUD service-management feature was built and verified, the user stated "layanan tidak bisa ditambahkan atau hapus, hanya edit dan nonaktif" and expected the assistant to rework type, store, consumers, admin UI, and docs end-to-end with no debate, no proposal, and no review of the previously shipped version. Re-confirmed with a scrape deliverable: after the assistant shipped an 18-post image-only result per a strict spec reading, the user replied "ahh okayy revise, use all 181 total post no matter if it video!" and expected the full revised output (all posts, videos included, schema reworked) without a proposal step. Confidence: 0.8

- Expects commits to stay scoped to the current task: when unrelated pre-existing changes are already in the working tree (e.g., from a previous session), stage and commit only the files relevant to this task rather than folding everything into one commit. But an explicit "commit all changes!" instruction overrides scoping — everything gets staged and committed as one comprehensive commit even when the work spans multiple epics (re-confirmed with "commit all changes then push": a single catch-all commit covering many epics, followed by push). Fresh confirmation: for a bare "push it" the assistant staged only the two source files of the domain change and deliberately left auto-managed `.commandcode/taste/` files uncommitted, then pushed — accepted without objection. Fresh confirmation: for a bare "okay commit and push our changes" the assistant reviewed the pre-existing uncommitted diffs (availability caching, pool bounds, no-304 response fix, site URL default), judged them coherent with the session's work, and bundled them into the same commit under an "Infra (pre-existing, bundled)" section while still excluding the `.commandcode/taste/` auto-managed files — the inclusive reading of "our changes" (session work + coherent pre-existing work in one commit) was accepted without objection. Fresh confirmation: for a bare "okay commit then push" the assistant staged only the two task source files, deliberately left the auto-managed taste files uncommitted, and pushed — accepted without objection. Confidence: 0.85

- Wants the assistant to apply fixes directly once a problem is diagnosed, rather than stopping at explanation — explicitly directs action with terse commands (e.g., "fix it") and expects the fix to be implemented and verified, not just described. Re-confirmed when a runtime error was pasted with no instruction: the assistant diagnosed the root cause, applied the minimal fix, typechecked, and committed without asking. Fresh confirmation: after the assistant's landing-page audit flagged the logo-upload gap (backoffice saves a logo that header/footer never render), the user replied "1. fix it then" and expected the gap wired end-to-end without further discussion. Confidence: 0.8

- When the landing page has in-page sections that match nav links, prefers the nav to smooth-scroll to those sections (with active-section highlighting and URL-hash persistence via `history.pushState` so back/forward and reload state work) instead of sending users to separate pages — applied to both desktop nav and the mobile sheet, with links that have no matching landing section (e.g., Gallery) still going to their full page. The user greenlit this UX direction with the bare "go!" approving the audit's recommendation. Confidence: 0.55

- Expects owner-managed settings values to be used consistently across every public surface that renders the same data, with static `SITE` constants only as fallback: when the footer already read `settings.businessProfile.mapsUrl` but the landing contact section hardcoded `SITE.mapsUrl`, the inconsistency was treated as a bug worth fixing ("matches how the footer already handles it"), not as separate acceptable behaviors. Confidence: 0.5

- Prefers the assistant to leave no dead code behind even from its own just-written work: mid-implementation it added a `LANDING_SECTIONS` const that turned out redundant (the nav helpers read `link.hash` directly) and immediately removed it "to keep things clean" before finalizing — the user accepted without objection. Confidence: 0.3

- Reports bugs by pasting the raw runtime error verbatim (stack trace + code frame, e.g., a Next.js TypeError with the offending line) with no prose or instructions — the pasted error itself is the request, and the assistant is expected to diagnose the root cause and fix it end-to-end. Fresh confirmation: pasted a structured build-error report (Error Type / Error Message / Build Output) for a Next.js Turbopack "Can't resolve 'tailwindcss'" CSS error with zero additional context, expecting the same diagnose-and-fix treatment. Fresh confirmation: pasted a structured runtime-error report (## Error Type: Runtime Error / ## Error Message: "Internal Next.js error: Router action dispatched before initialization" for Next.js 16.3.0 Turbopack) with no instructions, and the assistant diagnosed the cause (mount-time `router.replace` on an SSG page dispatching before the App Router's action queue initializes), applied the minimal fix (skip URL-sync effect on first render), and verified via typecheck, lint, tests, build, and headless-browser checks. Fresh confirmation: reported a UX bug as a terse prose symptom with no instruction ("got ux issue in gallery page, it show this when the data still load: 'Belum ada desain yang cocok' ... instead of loading") — the symptom report itself was the request, and the assistant diagnosed the root cause (provider starts with `[]` + `isLoading=true`, so the empty state rendered during fetch) and fixed it end-to-end (skeleton while loading, empty state only after load) without asking. Fresh confirmation: pasted a bare server log line ("POST /api/v1/contact-messages 500 in 62ms (next.js: 22ms, application-code: 40ms)") with no prose — the assistant traced it to schema drift (migration 0023 adding `contact_messages.instagram` had never been applied to the remote DB), applied the missing migration directly via psql, verified with a real POST (201) against the running dev server, and cleaned up the test row, all without asking. Fresh confirmation: reported a production config misbehavior as a terse statement with a context fact ("i already set NEXT_PUBLIC_SITE_URL=https://denailss.beauty but after login backoffice the redirect is to localhost:3000") and no instruction — the assistant traced where the redirect is actually constructed (env var usage across the codebase, auth/callback route, middleware, env files) and diagnosed the true source of truth (Supabase dashboard URL configuration, not app code or the env var, which is never read) rather than assuming the env var was honored. Fresh confirmation: reported an edit-modal data bug as a terse symptom with a file path and no instruction ("there is error in @src/app/backoffice/services edit, the modal not show correct data (different data)") — the assistant traced the root cause (the form component stays mounted across opens and only reads its `initial` prop in `useState` initializers, so switching from one service to another kept showing the previous record's data) and fixed it end-to-end by keying the form with `key={editing.id}` to force a fresh remount per record, then verified with `npx tsc --noEmit`. Fresh confirmation: pasted two bare dev-server log lines ("GET /api/v1/availability 500 in 29ms ... GET /api/v1/reviews 500 in 217ms") with zero prose — the assistant verified both endpoints against the running dev server, checked processes/ports/logs, and diagnosed the 500s as stale compiled-server state from the pre-fix build rather than a code bug, reporting the evidence and the restart command. Fresh confirmation: reported a save failure by pasting dev-server PUT log lines plus a file-path pointer and a terse symptom ("check the page @src/app/backoffice/availability i cannt save ... PUT /api/v1/availability 422 in 105ms ... but the ui said success") with no instruction — the assistant traced the GET→edit→PUT round-trip against the zod schema and DB column types, found the root cause (Postgres `time` values returned as "HH:MM:SS" failing the schema's HH:MM regex), normalized times at the service layer, typechecked, and verified the round-trip live. Fresh confirmation: pasted two bare dev-server log lines again ("GET /api/v1/availability 500 in 32ms ... GET /api/v1/availability 500 in 47ms") with no prose — the assistant isolated the hang to the `cachedApiSuccess` routes (while the `apiSuccess` route responded instantly), proved the code correct with a clean typecheck + full production build and a prod-server smoke test on port 3010 (all previously-hanging endpoints returned 200), and diagnosed the dev server itself as wedged, recommending a restart rather than a code fix. Fresh confirmation: pasted a full browser network-tab listing (many `(pending)` GETs plus 500s across the backoffice providers — bookings, availability, deposit-config, reviews, promotions, services, gallery, settings — and the `__nextjs_original-stack-frames` poller) with only the question "why there are many pending endpoints" — the assistant treated it as the same wedged-dev-server symptom, killed the stale process tree, started a fresh `npm run dev`, and verified every previously-pending/500 endpoint returned 200, explaining that the browser's request queue was against the dead server. Fresh confirmation: reported a backoffice form-fill bug as a terse symptom with a file-path pointer ("in @src/app/backoffice sometimes the data/form filled not correctly e.g in 'Aturan Booking' i need to open other menu first then open the Aturan Booking to make the form filled") — the assistant traced it to an async-hydration race (the rules/deposit fields were initialized via `useState` initializers with defaults while the config hydrates from the API, so the form stayed pinned to defaults until a menu switch remounted the component) and fixed it end-to-end by re-syncing form state from the hydrated config in `useEffect`, verified with typecheck + lint. Fresh confirmation: asked a terse "why" question about observed calendar behavior with context facts ("why 15 august 2026 in booking page is 'penuh', i already override, btw there is appointment in that date") — the assistant verified the actual data (override present and read, confirmed vs. cancelled appointments, buffer-inclusive slot math) and delivered an evidence-based verdict (the override is honored; the confirmed appointment plus its 15-min buffer genuinely saturates the 2-hour window, so "penuh" is correct behavior, not a config bug) rather than assuming the override was broken, then offered concrete follow-up options. Fresh confirmation: after the same review surfaced two issues (stale slot UI after a booking is created — the exported `useAvailabilityRefresh()` hook was never called post-submit — and a "HH:MM:SS" vs "HH:MM" format inconsistency in `getBookedSlotsByDate`), a terse "go fix" directed the assistant to fix both end-to-end: normalize `start` to "HH:MM" for uniform occupied-slot formats and call the availability refresh after a successful booking POST, then verify with typecheck + lint. Fresh confirmation: pasted three Next.js browser console warnings about `fill` Images missing the `sizes` prop (with zero prose or instruction) — the assistant added `sizes` to the reported hero avatars, swept the codebase for every other `fill`-without-`sizes` Image, and re-ran the typecheck; the pasted warning was the entire instruction. Fresh confirmation: after that `sizes` fix landed, reported the resulting regression as a bare four-word symptom ("the hero section layout now broken") with no file path and no instruction — the assistant diagnosed the root cause on its own (the motion wrapper renders `display: contents`, so the hero's text elements became direct grid children, each occupying its own grid cell), restored the left-column wrapper, rewrote the visual column cleanly, and verified with typecheck + lint. Confidence: 0.99

- Delegates next-step/roadmap planning to the assistant: asks "whats next" while pointing at the project's progress doc, expecting the assistant to read the doc, check existing plans and recent commits, and propose the next concrete epic/task (with a suggested approach) rather than asking the user what to do. Repeatedly confirmed — asked "what's next" again with no extra context (then "so what's next?" once more after an epic wrapped) and expected the assistant to pull next steps from `docs/PROGRESS.md` on its own. Confirmed again when the assistant proposed SEO polish as the next item: the user replied with a terse go-ahead ("okay GO to the SEO polish!!!") rather than re-planning; and once more with a bare "okay then what's next" right after the service-tiers epic wrapped; then approved the next roadmap epic (deposit cleanup) with a bare "go"; greenlit the following epic (deposit-config persistence, the last config mock) with a bare "continue!"; then greenlit the next roadmap item (overlap-aware booking validation) with "go! continue" and let the assistant pick, implement, verify, and doc it end-to-end; again replied just "continue" after a milestone summary and let the assistant pick, implement, verify, and commit a full lint-cleanup pass end-to-end. Confidence: 0.98

- Drives backlog expansion directly from the product: identifies missing admin menus/features terse-ly as follow-up requests after an epic ships ("next, ada menu yang kurang: menu untuk manage services layanan, menu untuk insert instagram last post (input embeded link)"), expecting the assistant to map the blast radius and build both additions without a formal proposal step. Confidence: 0.5

- Accepts (implicitly greenlights) the assistant's standard verification protocol — typecheck, lint, production build, and dev-server smoke tests of new routes — as the definition of done for shipped features. Fresh confirmation: after the landing-page changes (ISR, settings prop-hoisting, lazy-load hints) the assistant verified with `npx tsc --noEmit` and a production build whose route table showed `/` as Static/Revalidate 5m, and the user accepted the result without further requests. Confidence: 0.5

- Appreciates the assistant using plan mode for a large, decision-heavy migration: when a big chunk of work (e.g., migrating the last mock domains) has pivotal either/or decisions, the assistant entered plan mode, asked a single concise decision set up front (via structured multi-choice questions), then wrote a detailed plan file (`~/.commandcode/plans/...`) covering schema, repositories, routes, consumer migration, deletions, type changes, verification steps, and explicit non-goals — and the user approved it without changes ("continue"). This exact cycle repeated across four consecutive milestones in one stretch of work (catalog persistence, promos/reviews/favorites, CRM/finance/analytics, availability config) — each time the user answered the scope questions, then approved the plan tersely. Confidence: 0.65

- Expects backend migrations to preserve a usable frontend during incremental rollout: keep explicit local/mock fallback seams until infrastructure credentials and deployment setup exist, while making the API authoritative when configured. Confidence: 0.8

- Prefers building features front-end only on the existing mock data seam, deferring backend wiring to a later epic (explicitly said "do FE only") — mock-first implementation behind a named seam is the accepted interim until a real repository layer exists. Re-confirmed when offered "start the backend phase or pick the highest-value FE gap": chose to close the FE gap first ("okay fix the gap first") rather than begin the deferred backend. Confidence: 0.65

- When beginning backend work, wants the documented plan used together with the already-implemented frontend as the source of truth: inspect real UI/data seams and preserve existing component behavior and contracts instead of implementing from docs alone. Confidence: 0.85

- Agrees to keep user-uploaded/runtime-generated artifacts out of version control (approved adding `public/images/uploads/` to `.gitignore`) — generated storage, unlike source assets, should be gitignored so it doesn't get committed/deployed. Confidence: 0.5

- Frames UI-change requests as global/comprehensive: expects the change applied to every instance of the pattern in scope (e.g., "modif seluruh ui table in @src/app/backoffice" — all tables in the folder; "ketika approve atau reschedule ... dan juga di appointment detail" — the WhatsApp notify option on both approve and reschedule flows across the detail view and calendar; "di halaman overview ... tampilkan modal 'Beri tahu customer'" — the same deposit-verification notify flow requested on the overview page too; "di halaman drawer kalendar, tambahkan juga button whatsapp serta button menuju halaman detail appointment" — the same WhatsApp + detail-link buttons on the calendar drawer), not just a single occurrence; the assistant is expected to locate all instances and update each consistently, and the user keeps requesting the same flow on each new surface as it comes up. Confidence: 0.9

- Explicitly requires the requirement documents (PRD and TRD), not just the progress log, to be kept in sync when an implemented change alters an existing requirement — stated directly: "update the @docs about our progress also TRD and PRD if we have change the existing requirement!!!". Expects the assistant to audit which docs are affected by the change and update them (e.g., TRD skipped only when it genuinely doesn't define the changed detail), and to note requirement changes explicitly (what was removed/changed and why). Confidence: 0.85

- Proactively codifies cross-cutting UI standards into the TRD as mandatory requirements so every new feature complies from the start — explicitly asked to add a "setiap table wajib punya pagination" requirement to the TRD because newly created features (like the promotions table) were not automatically including it; prefers documenting non-negotiable standards/checklists up front over retrofitting them feature-by-feature later. Confidence: 0.8

- When asked "so what's next?" after an epic ships, expects the assistant to lay out the remaining roadmap from the progress doc in priority order (with scope notes, e.g., which epics are FE-only on mocks, deferred backend work) and end with a concrete either/or choice (e.g., "start Epic 7 (Finance) next, or knock out SEO polish first?") — presenting a short prioritized menu rather than asking open-endedly. Confidence: 0.7

- Challenges delivered work with terse, pointed questions in Indonesian ("btw apakah tidak perlu ada feature hapus promo?") and expects the assistant to defend the design with concrete reasoning (requirements brief, data-integrity tradeoffs) and then immediately act on the conclusion — here, removing the now-unused `deletePromotion` function to keep the codebase free of dead code. Fresh instance: questioned the phone-based guest-claim design ("hmm why not claimm based on email? not by phone") and the assistant defended with concrete data-integrity reasoning (recycled/shared phone numbers → false claims) then rewrote the logic to email-only claiming end-to-end. Fresh instance: followed up on the login-time claiming with another terse challenge ("hmm why need linked in login? why not when create booking then check is the customer/user with the email exist if yes then link") and the assistant reworked `createBooking` to link at creation time with login claiming demoted to fallback — the question itself is the instruction to adopt the proposed alternative. Confidence: 0.5

- When offered a prioritized menu of next steps (e.g., commit-first, start the deferred backend phase, or close the current phase's feature gaps), picks closing the loose ends/gaps of just-shipped FE work over housekeeping and over starting the next large phase — replied "okay fix the gap first" (tier-aware booking pricing) and again "go fix thegap!" (customer review submission), each time expecting the named gap implemented end-to-end without further planning. Prefers functional completion of in-scope work before moving on. Confidence: 0.75

- Decisively defers/skips proposed roadmap scope that would require significant new infrastructure or a new strategy (e.g., "skip it" for the API/RLS/E2E test layer, which would need a test-DB setup) rather than taking it on partially — expects the item to be marked as deferred by owner decision in the progress doc and the roadmap closed out as complete, without the item lingering as planned future work. Confidence: 0.55

- Uses the requirement docs (PRD/TRD) as a source-of-truth checklist and proactively requests audits to find features that are unimplemented or have gaps ("check @docs is there task/feature that not implmented or have a gap") — expects the assistant to verify each documented requirement against the actual code with evidence (file paths), separate genuine feature gaps from structural/mock-data gaps, and report findings grouped by severity with a recommended fix priority. Confidence: 0.6

- When a docs audit finds multiple gaps, wants them recorded in `docs/PROGRESS.md` first as a "Known gaps" section and then fixed incrementally, one at a time, each in its own scoped commit, with the progress doc updated to mark each gap resolved (and by-design decisions explicitly left as-is and documented as intentional) — instructed with "write the gap in @docs/PROGRESS.md then do it one by one". The assistant executed exactly this pattern (gap doc commit → 10 per-fix commits → docs "all resolved" commit) without objection. Confidence: 0.85

- Considers a warning-free `npm run lint` part of a production-shaped baseline: after a milestone landed with ~36 pre-existing lint warnings, the user's bare "continue" implicitly approved a dedicated pass to clear every unused import and `no-img-element` warning so the lint output is fully clean, then commit it. The user did not object to this being picked as the next task. Confidence: 0.5

- Accepts `eslint-disable-next-line` comments (with a brief reason) as the right resolution for intentional raw `<img>` usage that cannot use `next/image` — e.g., signed URLs that expire or fallback placeholder images — rather than converting or removing them. Confidence: 0.6

- Follows the repo's established lint-workaround convention for intentional `setState`-in-effect sync patterns: extracts the sync into a helper function so each `useEffect` has a single setter call behind one `// eslint-disable-next-line react-hooks/set-state-in-effect` comment (matching `dashboard-view.tsx`) — keeping the effect's logic intact while staying lint-clean, rather than scattering per-line disables or suppressing the whole file. Confidence: 0.55

- Prefers to keep deployment/infra work out of scope until the application itself is complete: when deployment (Vercel project, env vars, prod Supabase migrations, OWNER_SETUP — work that involves the user's own accounts/credentials) was recommended as the next milestone, decisively redirected with "skip the deployment, focus on the application first" — app-level completeness (gap audit + polish/hardening) takes priority over deploying. Confidence: 0.8

- Requests audits of the application code itself for user-visible functional gaps, not just docs audits: "is there a gap?" after a milestone — expects the assistant to sweep real risk areas (booking-flow edge cases, API route error handling, auth/route guards, empty/loading/error states) and report concrete bugs with severity, fixing what's found in the same pass (e.g., the Instagram proxy route's unhandled upstream fetch error → clean 502). Confidence: 0.5

- Treats repo-level polish artifacts as real gaps to fix, not nice-to-haves: when offered polish/hardening items (README still create-next-app boilerplate, incomplete `.env.example`, stale `supabase/seed.sql` comments claiming Storage "not implemented"), approved them all with a terse "fix th polish/hardneing" — expects README/env docs/seed scripts to accurately describe the current app (stack, scripts, migrations, env vars, what the seed covers vs. what migrations seed) rather than drift from reality. Confidence: 0.6

- Asks pointed verification questions about whether a shipped flow's data actually propagates end-to-end and whether its business rules actually enforce — e.g., "check the flow booking availabiliy (jadwaL) with the data backofice and also the pending/cancel appoinment — do that really block" — expecting the assistant to trace the whole chain (backoffice config save/read path → booking availability provider → slot engine → server-side conflict gate), enumerate exactly which appointment statuses reserve vs. free a slot (pending/confirmed/waiting_verification/pending_deposit block; cancelled/no_show/completed free), and deliver an evidence-based verdict on whether the UI and server-side checks agree — not a restatement of the intended design. Confidence: 0.6

- When fixing a runtime bug, prefers the minimal, surgical fix that matches the pattern already used elsewhere in the codebase — e.g., unwrapping `avail.data.config` in the backoffice context exactly as `availability-provider.tsx` already does — rather than a broader refactor; and expects the fix committed separately with a conventional-commit `fix:` message and a detailed root-cause body. Confidence: 0.45

- When asking for a review of the current app state (e.g., "review the landing page image, there are how many static image? write the summary"), expects a precise quantitative answer (exact counts, e.g., 22 static renders from 20 unique local files vs. 6 dynamic/proxied images) backed by evidence — distinguishing static local assets from dynamic/proxied ones, noting where the mapping is defined, and flagging caveats (e.g., seeds that flip from local to DB-hosted) — delivered as a written summary without further prompting. Confidence: 0.35

- Deploys through Vercel with auto-deploy on push to `main`: a terse "commit then push (will auto deploy in vercel)" means pushing to main ships the change to production automatically — no manual deploy step, and the assistant can simply commit, push, and report that Vercel will pick it up. Confidence: 0.8

- Issues terse, no-context execution commands for database/infra work — e.g., a bare "run the migration" after a schema change — and expects the assistant to autonomously own the whole flow without asking how: investigate the project's migration setup (package.json, drizzle/supabase config, env), determine the apply method, confirm the migration isn't already applied, apply it safely to the live database, verify the result, and report. Comfortable with the assistant operating directly against the live DB. Fresh confirmation: on a bare "run the migration" the assistant checked the drizzle journal + DB tracking tables first, ran `drizzle-kit migrate`, verified every hand-authored migration's artifact directly in the DB, and reported the schema was already fully applied — and the user accepted this state-of-the-DB conclusion as the answer without pushing to force any bookkeeping. Confidence: 0.7

- When asked to "run the migration," expects a state-of-the-database conclusion (verified per-migration artifact checks: tables, columns, seed rows) rather than a literal execution, when the migrations are in fact already applied — the assistant determined the hand-authored migrations were applied directly outside the drizzle journal and reported "nothing is pending" with a caveat about fresh-environment setup. Fresh confirmation: the same bare "run the migration" was repeated verbatim and the assistant again concluded "it's done" — this time via a per-file audit of all 22 migrations covering tables, columns, and seeded rows (promotions, instagram posts, deposit_config, booking_rules, vacations) plus confirming `supabase db push` was unusable because the project was never linked — and reported the feature was live end-to-end rather than forcing any bookkeeping. Confidence: 0.55

- Wants project analysis/artifact documents placed inside the project repo (e.g., `docs/instagram-analysis.md`) rather than left in temp/scratch locations — "move the analysis md to this project" — treating scrape/classification analysis as versioned project documentation that ships with the code. Confidence: 0.5

- Before running a data-seeding script against the live database, checks up front whether it removes existing data ("do the script remove the existing data first?") and expects a precise breakdown of what the script deletes vs. upserts — prefers non-destructive, incremental re-runs that leave unrelated rows untouched rather than wipe-and-reseed. However, when legacy seeded data from a previous seeding path lingers (e.g., old `des-*` rows from a superseded `seed.sql`), explicitly wants it removed rather than left in place ("i mean there are seeded data before this, we need to remove it") and accepted a `--reset` flag that deletes previously seeded rows (legacy `des-*` + prior `ig-*`) before seeding. Fresh confirmation: "remove all promosi, just seed 3 data" — every previously seeded promo deleted and replaced with a minimal 3-row set (1 inactive + 2 active, empty images) in both the migration and the live DB. Confidence: 0.65

- Runs delivered seed scripts/deliverables against the live environment themselves (with their own credentials) and reports back terse numbered findings — mixing correctness questions ("the name has postfix '#number', is it expected?") with concrete bug reports ("in edit gallery backoffice, the data not prefilled with selection edit") — expecting the assistant to address each item: justify expected behavior with concrete reasoning and fix genuine bugs end-to-end. Confidence: 0.5

- After an asset/data-flow accounting, probes every content surface with pointed per-item questions about its actual data source ("hmm the logo not dynamic? in backoffice setting we can upload logo", "does featured design from gallery?", "does services use from backoffice?", "does promotion banner use from backoffice?") — expects the assistant to verify each against the real code paths (providers, API routes, upload/submit payloads) rather than restate the earlier summary, and to report an honest dynamic-vs-static picture that explicitly flags backoffice features which save data but never render it publicly (e.g., the settings logo upload that header/footer hardcode around) as a real gap/bug, not a non-issue. Fresh confirmation: after learning promo banners are DB-backed, followed up with a bare "the image, get from where" to pin down the image source (DB seed vs. uploaded storage vs. local fallback), expecting the resolved-at-runtime picture with all cases enumerated. Re-confirmed with "why still promo weekend nail date have image? or is the default image? please change it if yes" — another pointed probe of the same promo image source, expecting the fallback confirmed and removed once identified. Fresh confirmation: right after a performance fix landed for the Instagram feed images, probed the remaining image surfaces in the same terse per-item style ("what about image from gallery, services, gallery have 181 thumbnail images") and accepted the evidence-based verdict that those images were already optimized while the real weight lay elsewhere (uncached catalog fetches). Fresh confirmation: probed the seed-image map itself — "check @src/lib/images.ts do we use all image in SEED? especially services, bukannya kita pakai dari db?" — asking whether every SEED entry is actually consumed and whether services data really comes from the DB; expects the assistant to trace each seed key to its runtime consumers and report which entries are live (DB-backed values resolved through the seed map) vs. legacy (kept only for migration backfill), rather than restating the map's contents. Fresh confirmation: immediately followed with "what image in landing page that still static?" — probing which landing images remain hardcoded vs. DB-driven, expecting a precise per-surface breakdown (hero main/detail, about, contact, and review avatars static; services, featured designs, promo banner, reviews, Instagram grid dynamic) with evidence of where each is defined and resolved. Confidence: 0.92

- Treats client-fetched sections that render empty until the async fetch completes as a bug — reported tersely as "it causes bug when apply filter" when the reviews section showed no cards and no filter buttons until hydration/services loaded. Prefers server-side seeding of the initial data (page-level fetch passed as `initialReviews`/`initialSummary` props) so SSR renders content, summary chips, and filter options on first paint, with the client provider refreshing live data on top — rather than an empty-until-mounted section. Fresh confirmation: reported the gallery page showing the "Belum ada desain yang cocok" empty state while data was still loading ("it show this when the data still load ... instead of loading") — expects a loading indicator/skeleton during the fetch and the empty state only after loading finishes, not a false "no results" message on first paint. Fresh confirmation: after the skeleton fix, reported "loading in gallery page is too long!" — a skeleton during a full client-side fetch is not an acceptable end state; the accepted fix converted the gallery page into an async server component that fetches the catalog during SSR and passes `initialDesigns` to the explorer (the same pattern the landing page already uses), so all content renders in the HTML on first paint with zero client-side waiting while the client provider re-fetches/refreshes on top. Confidence: 0.75

- Proactively initiates page-level performance/improvement audits with a terse open-ended question even when nothing is reported broken — e.g., "check the landing page, is there any improvement/performance that we can apply" (and previously "review the landing page image, there are how many static image? write the summary") — expecting the assistant to read the page's actual components/services/providers/config and deliver a comprehensive, ranked analysis (architecture-level: caching/ISR vs dynamic SSR, repeated DB reads, client-side double-fetching, proxied image requests, image loading hints) ending with a concrete "what I'd actually change" implementation offer rather than just a verdict. Fresh confirmation: asked "now can we improve the public page? is there possibility improvement?" right after a commit/push wrapped — again an open-ended audit question with nothing reported broken; the accepted response read every landing section (hero, about, contact, FAQ, featured designs, reviews, header/footer, layout, Instagram, settings wiring) and delivered an honest verdict ("strong shape, refinement not rebuild") plus ranked, concrete, non-slop improvements (in-page anchor nav, settings-driven mapsUrl, mobile nav parity, Instagram feed weight) ending in a scoped implementation offer ("Want me to implement those two?"). Confidence: 0.6

- When the user flags a performance problem, expects the whole weight profile investigated — every provider, every fetch, every cache header — not just the single suspected surface: the assistant traced all 7 global providers in the root layout (each firing uncached `fetch` on mount and tab focus, re-downloading the full catalog) and treated that systemic finding as the real fix, with verification via typecheck and lint. A fresh instance confirmed the expected pattern: diagnose beyond the user's hypothesis, apply the minimal systemic fix, verify, then report. Confidence: 0.6

- Reports performance problems as a terse symptom with a guessed cause ("the website so heavy, i think it because the image") and expects the assistant to verify the guess with hard evidence (measured file sizes via du/ls, traced load paths through proxy routes and next.config) rather than accept it at face value — here the site's own images were small (26-98KB, already optimized) but the real weight was the Instagram proxy serving full-res `?size=l` images with `unoptimized`; the fix removed `unoptimized` from both feed components so next/image resizes/caches them, verified with typecheck + lint. Fresh confirmation: reported a stale-data bug the same way ("there is issue i need hard refresh to get my booking (portal customer), i think we dont need cache it and also in booking flow get availabity") — the assistant verified every caching layer (route handlers, middleware, next.config, providers, services, auth, Supabase) rather than accepting the guess at face value, and found the real root cause (ETag/304 revalidation whose body-less 304 responses client reads treated as failures, silently keeping stale state) instead of the guessed plain-caching issue. Confidence: 0.65

- Expects a data-field addition to a customer form to be traced end-to-end and applied to every surface that touches that data — when asked to "add instagram username" to the contact form, booking data-diri form, and My Profile, the assistant (without objection) also updated the DB schema + hand-written idempotent migration, shared types (ContactMessage, AppointmentCustomer, CrmCustomer, CustomerProfile), service/API layers, backoffice views (messages inbox, CRM list/detail, appointment detail, calendar drawer), and CRM search — a single field change shipped as one complete cross-cutting pass rather than only the three named forms. Confidence: 0.6

- Accepts the assistant's standard data-flow change protocol without debate: new hand-written idempotent migration (numbered after the latest), Drizzle schema, types, zod schemas, services, API, UI, then `npx tsc --noEmit` and `npm run lint` as the definition of done — with pre-existing lint warnings explicitly checked to be unrelated before reporting. Confidence: 0.45

- When the assistant hands over how-to instructions or a testing checklist (e.g., SEO smoke-test steps), a terse "please test it" means the user wants the assistant to execute the verification itself against the live deployment — curl the endpoints, inspect rendered title/meta/canonical/JSON-LD/OG tags, sitemap, and robots.txt — distinguish real gaps from test artifacts (e.g., verifying dynamic routes with real slugs pulled from the sitemap rather than guessed ones), fix genuine issues found in the same pass, and report a complete pass/fail picture instead of leaving the testing to the user. Fresh confirmation: after the canonical-tag fix was pushed ("commit then push (will auto deploy in vercel)"), a bare "check again" meant re-run the live-site verification once the deploy landed — confirm the canonical now renders on homepage/gallery/service pages, follow redirects (noting the sitemap 308 → www), distinguish real gaps from test artifacts (real slugs pulled from the sitemap), and report the pass/fail summary including any remaining inconsistency found (www vs non-www canonical host). Confidence: 0.7

- Provides terse factual corrections with no verb (e.g., "btw my domain is denailss.beauty") as implicit instructions, expecting the assistant to treat the correction as a codebase-wide consistency task — verify every place the fact is encoded (the `SITE.url` constant feeding canonical URLs, sitemap, robots, and JSON-LD; hardcoded contact email), fix mismatches, and sweep for stale references before reporting — rather than just acknowledging the information. Confidence: 0.55

- Iterates on code quality by having the assistant self-review its own just-written code for consistency between comments/intent and actual behavior, then fix what's found before finishing: after implementing `claimAnonymousCustomer` with a comment claiming "matches by email first, then by phone" but code that only matched email, the assistant caught the mismatch on re-read, fixed the logic to truly claim by email OR phone (phone being the primary guest identity), and re-ran typecheck + tests. The user did not object to this self-correction pass. Confidence: 0.35

- When a fix is later simplified (e.g., a dead branch removed), expects the assistant to notice and clean up the resulting dead code itself before finishing — after the `cachedApiSuccess` 304-branch removal made the if/else branches identical, the assistant collapsed them into one response and then went further to drop the now-unused `request` parameter and update all seven callers, so no dead parameter or unused variable remained; the user accepted the result without objection. Fresh confirmation: while implementing the nav-anchor feature, the assistant noticed a `LANDING_SECTIONS` const it had introduced was redundant (the helpers read `link.hash` directly) and removed it immediately "to keep things clean" — proactive removal of its own unused code mid-task, accepted without objection. Confidence: 0.4

- When the dev server gets wedged (browser shows many `(pending)` requests + 500s across routes while the same code passes a production build), treats a server restart as the fix — kill the stale process tree and start a fresh `npm run dev` (in the background), then verify every previously-pending/500 endpoint returns 200 — rather than chasing further code changes; expects the assistant to distinguish wedged-server symptoms from code bugs using the prod build as ground truth and to just do the restart. Fresh confirmation: after the user pasted the pending-endpoint network listing, the assistant killed the wedged server, started a fresh `npm run dev`, and re-verified all endpoints returned 200, with the browser needing only a hard refresh against the new server. Boundary condition confirmed: when the same endpoint re-appears as intermittently pending/500 *after* a restart (user re-reports "still pending... why??"), the cause is a real code bug, not server state — the assistant reproduced under concurrent load, confirmed the hang on the production build, and traced it to connection-pool exhaustion from one endpoint firing 5 parallel DB queries per request. Confidence: 0.75

 fix — kill the stale process tree and start a fresh `npm run dev` (in the background), then verify every previously-pending/500 endpoint returns 200 — rather than chasing further code changes; expects the assistant to distinguish wedged-server symptoms from code bugs using the prod build as ground truth and to just do the restart. Fresh confirmation: after the user pasted the pending-endpoint network listing, the assistant killed the wedged server, started a fresh `npm run dev`, and re-verified all endpoints returned 200, with the browser needing only a hard refresh against the new server. Confidence: 0.7

