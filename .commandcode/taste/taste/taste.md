# Taste
- Prefers real, live data from the original/official source over static mocks or placeholders — even when it means extra setup work (e.g., asked to fetch real Instagram images and embed the latest post rather than keeping seeded placeholder images). Confidence: 0.8

- Wants scraped portfolio/dataset content enriched with AI vision-based classification expressed in the product's own domain vocabulary — asked for image analysis determining "style, warna, acara, bentuk, tingkat kesulitan" (exactly the gallery/booking taxonomy the site already uses), expecting the enriched attributes to be merged into the data and directly consumable by the app (gallery curation, booking filters, per-difficulty pricing) rather than kept as free-text notes. Confidence: 0.6

- Wants scraped/collected datasets to be complete and inclusive rather than filtered to a strict spec-literal subset: when the assistant strictly applied an image-only eligibility rule and reported only 18 of 181 Instagram posts, the user overrode with "ahh okayy revise, use all 181 total post no matter if it video!" — expecting the full dataset (all posts including reels and mixed carousels, with videos downloaded alongside images, and `media_type` per post) reworked end-to-end without debate. Prefers the complete picture over a conservative interpretation of eligibility rules. Re-confirmed when asking for the gallery seeding script, framing the expected scope as the full dataset ("181 data?"). Confidence: 0.8
- Wants the database to hold real data only, not fictional demo content: when scoping the mock-domain migration to Supabase, chose NOT to seed the fictional appointment/customer history into the DB (accepting that backoffice CRM/finance/analytics dashboards stay mock-backed and would look empty with real bookings only — "cleaner truth, emptier UI"), and prefers building a real customer-facing feature (a working favorites save/heart toggle) over persisting a fake static list whose "feature" stays fake. Reaffirmed with fresh evidence when the same either/or came up for the last mock domains (CRM/finance/analytics): chose "Real data only" again over seeding the fictional customer/appointment history, accepting initially-empty backoffice dashboards until real bookings accumulate. Confidence: 0.8

- When migrating features off mock/localStorage persistence, preserves full feature capability rather than trimming scope for simplicity — for the CRM migration chose a dedicated `customer_notes` table + `customers.preferences` jsonb (full notes editor + preferences card kept) over the minimal option of folding notes into an existing column and dropping the preferences UI. Confidence: 0.5
- Prefers lightweight, officially-sanctioned integration paths (e.g., pasting Instagram's "Copy embed code" into a component, or a backoffice input that accepts an embed link/code) over unofficial scraping or building heavy backend/Graph API infrastructure — favors no-backend, low-friction solutions that still deliver real content. Explicitly specified the embed-link input for the Instagram feature ("menu untuk insert instagram last post (input embeded link)"). Confidence: 0.8
- Doesn't accept "that's not possible" at face value — will research and paste official documentation to push a feature forward, and expects the assistant to verify feasibility before declaring something impossible. Confidence: 0.6
- Comfortable with a manual content-maintenance workflow for third-party embeds: will copy official embed codes (e.g., Instagram "Copy embed code") and paste the raw HTML into chat for the assistant to wire into the codebase, accepting paste-to-update maintenance (swapping shortcodes/data values) over building automated fetch-and-cache infrastructure. Confidence: 0.8
- Delegates subjective selection decisions to the assistant (e.g., "you choose it!" for which Instagram image to use; "tambah yang lain" — add other stats cards without naming which metrics) instead of prescribing exact content, trusting the assistant's judgment on details. Confidence: 0.6
- In structured scope decisions during migration planning, defers to the assistant's "Recommended" option on breadth — chose the full services migration (public + booking reads too, not admin-only), the promos+reviews+favorites milestone scope, full CRM notes + preferences persistence, and building a real favorites save toggle over persisting a static list — while reserving the data-truth call for themselves (picked "Real data only" over seeding demo history even when seeding was the recommended option). Confidence: 0.6

- Wants backoffice list/overview pages to carry a small band of multiple summary metric cards rather than a single stat — e.g., flagged that the gallery stats only had "Kesulitan Tinggi" and asked to add others, accepting assistant-chosen metrics derived from the existing data model (premium count from price, total photos from imageSeeds) with the grid expanded to fit. Confidence: 0.5
- Communicates in Indonesian with terse, direct feedback (e.g., "kurang 1 data" — "missing 1 data"; "tidak perlu ada tag juga" — "no tags either"), checking the delivered result against the expected dataset and pasting the exact raw embed/data needed to fill any gap. Confidence: 0.8

- Prefers conventional, well-structured commit messages (feat:/fix: scopes, detailed body with bullet points and co-author attribution). Confidence: 0.5

- Wants domain-accurate data modeling of real-world nuance — e.g., nail art is not a flat/static service but has difficulty levels (easy/medium/complex) that should drive per-design pricing, so data models should carry that variability (e.g., a `difficulty` field) instead of single hard-coded prices. Confidence: 0.6

- Prefers dedicated, separately-managed settings fields for distinct user-facing values over deriving one value from another or leaving it hardcoded — explicitly chose "seperate field, lang + lat pair" (a dedicated, range-validated latitude/longitude pair with SITE fallbacks) for the embedded map on the contact page, keeping it independently editable from the maps directions link. Confidence: 0.5

- Expects business settings to ship with seeded real defaults in the seed data — e.g., asked to "add seed for pengaturan": the Google Maps link plus latitude/longitude — so map surfaces on the public site render real values out of the box rather than being empty/unset until the owner edits them in the backoffice. Confidence: 0.45

- Treats SEO as a first-class, explicitly-important concern: flagged the SEO settings fields in the seed request as "SEO (the important)", and asks whether the SEO implementation is complete and how to test it — expecting a per-surface completeness audit (root metadata, per-page metadata + JSON-LD, sitemap.xml, robots.txt) that honestly names gaps, plus concrete validation steps (Google Rich Results Test, Schema.org validator, Search Console sitemap submission/URL inspection, social share-preview checks). Confidence: 0.5

- Prefers the displayed price to stay stable in the customer flow — selecting a design should not visibly change the price shown (the estimate/nominal stands and the final price is confirmed separately, e.g., via WhatsApp) rather than the summary updating per design click. Re-stated explicitly ("sepertinya tidak perlu berubah harga setelah klik desain"). Confidence: 0.7

- Expects thorough verification before finalizing a task — typechecking, git status/diff review, and a full picture of what's being committed — even when the user didn't explicitly ask for it. Confidence: 0.6

- Challenges proposed flows when the decision-maker doesn't match reality — e.g., the customer must never self-assess nail-art difficulty/pricing in the booking flow; difficulty is set by the artist/studio on curated designs and quoted by the artist for custom requests. Prefers flows where authority lives with the party who owns it in the real world. Confidence: 0.9

- Keeps every product surface consistent with the booking domain model — when the customer booking flow gains capabilities (multiple services per booking, press-on fulfillment choice, nail-art body part + free add-on), the backoffice and customer portal must render the same model. The user proactively audits for stale views and asks to sync them ("backoffice masih blm sync dengan changes yang sudah kita lakukan... please sync kan"; "what about in backoffice? detail appointmnt and manage services" — asking the assistant to verify the feature is surfaced on the backoffice appointment detail and the manage-services catalog). Confidence: 0.85

- Wants the promo/discount step to be visible for every service in the booking flow, not conditional on service type — explicitly insisted "step promo visible untuk semua layanan!". Confidence: 0.7

- Expects made-to-order/delivered items (e.g., press-on fake nails) to get a fulfillment/pickup step in the booking flow (ambil di lokasi vs dikirim via kurir), replacing the date/time slot that applies to in-studio services — and it should appear whenever fake-nail is selected, even alongside other in-studio services, not only when it is the sole service. Confidence: 0.8

- Wants multi-step booking/wizard flows to derive their steps dynamically from the current selection and skip steps that don't apply rather than showing empty or irrelevant ones — e.g., the Desain step only when nail-art, fake-nail, or gel-extension is selected, and skipped entirely for manicure/pedicure/removal-only bookings. Confidence: 0.8

- Press-on/fake-nail is a medium that can carry any nail-art design, so the design catalog must associate all designs with fake-nail — the "Pilih Desain" step must never render empty for a service that legitimately triggers it (flagged the empty design list for fake-nail as a defect to investigate/fix, not a data limitation). Confidence: 0.7

- Prefers the design-selection step to show the full catalog without service-based filtering — explicitly suggested dropping the `relatedServiceSlugs` filter ("sepertinya untuk pilih desain tidak perlu filter relatedServiceSlugs") since any design can be applied to any eligible service (including press-on); only search/style/color/occasion filters should apply. Confidence: 0.8

- Requires UI work to avoid generic "AI slop" aesthetics — explicitly demanded "donot make ai slop design" — meaning deliberate, design-system-consistent surfaces (dense scan-friendly layouts over default card grids, real images over placeholders, restrained accent use, full empty/loading/error state coverage) rather than default template output. Confidence: 0.6

- Prefers a lean feature set: features the user deems unnecessary get deleted outright rather than kept ("apa fungsi reset katalog" → "tidak perlu" → "hapus feature-nya"; "tidak perlu tanggal terbit" → the entire publish-date feature removed; "tidak perlu ada kategori" → the service `category` field removed entirely from the type, seed, admin form/list, and public surfaces rather than kept around unused; "no need SEED_MAP" → the promo entries deleted from the seed-image map entirely once promo banner images come only from backoffice uploads or the local fallback; the fallback itself was removed later once the user confirmed image-less promos should show none). Confidence: 0.85

- When a content item has no image, expects the surface to render without an image rather than substituting a bundled default/placeholder photo — questioned why the WEEKEND20 promo banner still showed a photo ("why still promo weekend nail date have image? or is the default image? please change it if yes"); the accepted fix made the banner render a full-width text-only slide when `imageSeed` is empty and removed the `promo-default` fallback from SEED_MAP entirely. Confidence: 0.7

- Wants seed data kept to a minimal curated set that still covers each relevant state — wiped all seeded promos and reseeded exactly 3 (1 inactive + 2 active) with empty images rather than keeping a rich fictional promo catalog; the seed exists to exercise states (active vs. inactive) and defaults, not to look complete. Confidence: 0.6
- Expects feature/data-field removal to be complete across every surface: the type definition, mock data, admin form (state + input + submit payload), admin list (column, sort options, statistics cards), and public-view usage (e.g., landing sort) — with no dead-code or stale references left behind, verified by grep + typecheck. The same thoroughness applies when a CRUD capability is removed and replaced (e.g., services: dropping add/delete meant removing create-mode branches, slug-uniqueness validation, `upsertService`'s create path, and `deleteService` — leaving the codebase dead-code-free). Confidence: 0.85

- Proactively questions whether a capability or data field belongs in scope at all — e.g., asked whether the promotions admin really doesn't need a delete feature ("btw apakah tidak perlu ada feature hapus promo?"), and later questioned whether the gallery's `aspect` field is needed ("the gallery has design-aspect, i think we dont need it right?"), expecting the assistant to justify scope decisions against actual usage (verify where the field/capability is consumed) and the requirements brief/data-integrity tradeoffs rather than reflexively shipping every CRUD operation or every data field. Accepts the field/capability without further debate once the assistant demonstrates it is genuinely consumed end-to-end (replied "okay keep it" for `aspect` after usage in cards/carousel/booking was shown). The same questioning applies to generated data artifacts: asked whether the "#N" title postfix on seeded gallery names is expected ("the name has postfix '#number', is it expected?") and, once the assistant showed it guarantees title uniqueness (many posts share the same style+color+occasion combo) and offered alternatives, chose to keep the format as-is. Confidence: 0.55

- Wants all owner-manageable images (including catalog/design photos) to be upload-only and stored in Supabase Storage buckets, with support for replacement, reordering, and deletion; admin forms must not offer bundled/seed stock images as selectable content. Static assets without a content-management flow, such as landing-page brand/marketing imagery, should remain bundled under `public/images`. Confidence: 0.95

- Expects admin-managed content to be wired end-to-end into public customer surfaces, not kept backoffice-only: uploaded designs must show up live in the public gallery catalog and in the booking/order design step ("tampilkan pada katalog dan order items"), via a shared live-catalog seam rather than static seed data. Also expects admin-set fields like the custom `price` to be rendered to customers on the public gallery card and detail page ("tampilkan di card galery dan detaik"), not just stored. Fresh evidence: when reviewing the landing page, the user probed every backoffice-driven surface (logo, featured designs, services, promotion banner) to confirm each is actually dynamic, and treating a logo uploaded in settings that header/footer never render as a defect — then commanded the gap fixed ("fix it then"), expecting the public header/footer to render the uploaded logo with a bundled fallback until one is set. Confidence: 0.9

- Treats the backoffice as the owner's full content-management surface: every user-facing content type (gallery, services catalog, Instagram grid, settings, promotions) is expected to have an admin menu for self-service management rather than code-level edits — and the user flags missing menus terse-ly as build requests ("next, ada menu yang kurang: menu untuk manage services layanan, menu untuk insert instagram last post (input embeded link)"). Fresh confirmation: requested the public maps link be managed from the backoffice settings rather than hardcoded. Confidence: 0.7

- Wants admin control over the display order of user-facing content, not just what content exists — e.g., explicitly requested a feature to reorder uploaded design photos ("tambah feature untuk mengubah urutan gambar yang tampil"), with the ordering stored and respected by the public consumers. Later refined the reorder UX to drag-and-drop on a compact thumbnail grid rather than per-item move buttons. Confidence: 0.7

- Prefers compact, dense layouts in management UIs over spacious one-item-per-row lists — explicitly rejected a one-image-per-row layout ("jangan 1 gambar 1 row, buat compact seperti sebelumnya") in favor of a responsive thumbnail grid; for reordering prefers drag-and-drop (over move arrow buttons) and expects clicking a thumbnail to open a large preview/detail. Confidence: 0.8

- Wants admin pricing to be a custom free-text input rather than locked to an auto-derived value (e.g., design price always computed from difficulty tier) — the owner should be able to type any price, including overrides such as promos. Explicitly stated: "harga sebaiknya masuman custom saja freetext", then confirmed by fully removing the tier-derived price mechanism (`priceFrom`) and its sort/display surfaces in favor of the custom `price` field. Confidence: 0.8

- Wants the user-facing UI to never render invalid/broken values from stale data — flagged "RpNaN" on gallery cards as a defect; expects old persisted data (e.g., localStorage entries saved before a field like `price` existed) to be migrated automatically on load with sensible fallbacks, and formatting helpers to be defensive (non-finite/negative amounts render "—" instead of "NaN"), rather than telling the user to reset their data. Confidence: 0.5

- Wants data tables in admin/backoffice UIs to expose a configurable "items per page" pagination selector (options like 5/10/15/25/50) defaulting to 10, instead of a hard-coded page size — explicitly requested "berapa banyak item per page pagination" with "default 10". Treats pagination as a mandatory baseline for every backoffice table, with no exception for small datasets (lists can grow), and expects it to be included when a feature is first built, not retrofitted — flagged the promotions table as non-compliant ("setiap buat baru feature, tidak otomatis menambahkannya"). Confidence: 0.8

- Prefers WhatsApp as the customer-communication channel woven into backoffice/admin UX: after consequential actions — approving a deposit, rejecting a manual deposit verification (with the rejection reason embedded in the pre-filled message), or rescheduling an appointment — offer a follow-up "Beri Tahu Customer"/"Beri Tahu di WhatsApp" option (modal or in-drawer panel) with a pre-filled notification message (plus a "Nanti saja" escape), and provide a WhatsApp chat button on admin surfaces (detail pages, and in the calendar drawer's customer profile card) — instead of leaving the operator to contact the customer manually. Explicitly asked for the notify step after BOTH outcomes of manual deposit verification ("setuju atau ngga"). Confidence: 0.9

- Wants new panels/sections inside a drawer or sheet to sit within the content container's padding and spacing system, not appended at the sheet edge — flagged the WhatsApp notify panel's spacing against the drawer as "suck"; the fix is to place it inside the same `px-* py-* space-y-*` wrapper as sibling sections so it inherits consistent horizontal padding and vertical rhythm instead of sticking to the edges. Confidence: 0.6

- Wants contextual/compact views to carry quick-action buttons that jump to the full related records — e.g., the calendar drawer's customer profile card should offer WhatsApp chat, appointment detail ("Lihat Detail"), and customer detail ("Lihat Customer"), explicitly asking to add the customer-detail button alongside the appointment-detail one ("selain detail appointment, tambahkan button detail customer juga"). Confidence: 0.65

- Business identity/terminology: this is a home-based nail art business ("nailart rumahan"), not a studio — all user-facing copy in the backoffice/product must use home/operational language ("rumahan", "operasional", "Tidak Buka") and never "studio" branding. The user audits for lingering mismatched terminology and expects a comprehensive sweep ("kenapa masih ada kata 'studio' di @src/app/backoffice? hilangkan karena ini nailart rumahan"), applied to every visible instance in scope — including settings/business-profile copy (e.g., a "Boutique nail studio..." description hidden in `settings.mock.ts` that surfaced only on a second, project-wide grep). Confidence: 0.9

- Reviews delivered UI at fine visual detail and expects input field adornments (currency prefixes like "Rp", icons) to be vertically centered and optically aligned with the field's own text baseline — flagged the Rp prefix in the nominal field as misaligned ("the Rp is not align (middle of field)") and re-checked the rendered page when the first fix didn't work ("still not!"). The real root cause: the base UI Input renders `display: inline-block`, which creates phantom line-box height below it in a plain `div.relative` wrapper, so any prefix centered against the wrapper (`top-1/2 -translate-y-1/2`, `inset-y-0`) drifts off the input's true center. The robust fix is a `relative flex items-center` wrapper + plain `absolute left-3` span, so the container's height matches the input's real box. Confidence: 0.8

- All user-facing product copy is written in natural Indonesian — UI labels, section titles, hints, empty states, validation messages (e.g., "Nama bisnis wajib diisi."), toasts ("Pengaturan berhasil disimpan."), and page metadata — including inside the backoffice admin (a settings feature built entirely in Indonesian was accepted without complaint). English appears only in code identifiers, not visible copy. Confidence: 0.6

- Treats mobile responsiveness as a baseline requirement for backoffice pages — reported the analytics page overflowing on mobile ("the layanan populer and jam booking favorite widht is wider then screen") and expects list rows with labels, counts, and progress bars to shrink/truncate within the viewport instead of forcing horizontal overflow (root cause: flex items without `min-w-0`). Reports such issues terse and by affected component, expecting a direct fix. Confidence: 0.6

- Mandates a non-destructive lifecycle for managed catalog records: explicitly directed that services cannot be added or deleted — only edited and toggled active/nonaktif ("layanan tidak bisa ditambahkan atau hapus, hanya edit dan nonaktif"). Deactivated records must survive for historical context (appointments, old links) while disappearing from booking and public surfaces — the assistant's split of `getLiveServices` (all, for backoffice history) vs `getActiveServices` (public/booking only), plus a kept-by-slug detail page with a "nonaktif" notice, was accepted as the right shape. Confidence: 0.9

- Cares about site performance/weight and expects user-facing images to be served through Next.js image optimization (resize to the layout's `sizes` width, WebP/AVIF, aggressive caching) rather than raw full-resolution files — flagged the site as "so heavy" blaming the images; the accepted fix removed `unoptimized` from the proxied Instagram feed images (which fetched Instagram's full-res `?size=l` and served them untouched, 1-3MB each), letting the built-in optimizer handle the `/api/instagram/*` local route. Raw/`unoptimized` full-res image delivery is treated as the first suspect when a page feels heavy. Fresh confirmation: after the Instagram image fix, the user immediately probed the other image-heavy surfaces ("what about image from gallery, services, gallery have 181 thumbnail images"), expecting the weight analysis to extend to every image surface (gallery, services) and the real inefficiency (uncached catalog data fetches, not the optimized thumbnails) to be found and fixed rather than stopping at the flagged one. Confidence: 0.7

- Cares about the network/transfer side of performance, not just asset size: when the images turned out fine, the next concern was the redundant data the browser downloads — and the accepted solution was ETag revalidation + `Cache-Control: no-cache` on public GET endpoints (cheap 304s on repeat/focus fetches) so freshness for admin edits is preserved while payload transfer drops to near zero. Long-lived responses get `s-maxage` (86400) with the HTTP cache keyed by CDN URL; short-lived/dynamic ones get `no-cache` revalidation so refetches return a 304 instead of re-shipping the full body. Confidence: 0.6

- Prefers pragmatic, keep-it-simple infrastructure decisions over planning for scale that isn't needed: explicitly settled the rate-limiting question with in-memory as the final approach ("for rate limitting us in-memory it's okay!") and had the distributed-limiter (Redis/Upstash) roadmap item removed outright rather than kept as future hardening work — no multi-instance infrastructure planned unless the app actually moves to multi-instance production. Confidence: 0.85

- Expects account uniqueness to hold across authentication providers: if an email has already registered through Google, signing up again with email/password using that same address must be blocked rather than creating or appearing to create a second account. Confidence: 0.95
- Expects authentication errors and recovery guidance to be provider-aware without assuming every existing account has a password: for Google-created accounts, explain that the user should use the previously used method and prominently support the Google login path rather than instructing them to enter a nonexistent password. Confidence: 0.95
- Prioritizes abuse protection for upload APIs: upload endpoints should have server-side rate limiting rather than relying only on client-side controls, with the protection applied consistently across relevant upload paths. Confidence: 0.9

- Prefers folding small owner-editable content into the existing single-row settings table (a JSONB column, edited from `/backoffice/settings`) rather than creating a new dedicated table + admin surface — when the assistant offered two ways to make the landing FAQ dynamic (a new `faq_entries` table vs. reusing the settings row's JSONB, matching how service FAQs already work), the user approved the lighter option with a bare "go!" and accepted the migration seeded with the existing copy plus a default fallback so the landing keeps rendering before the row is edited. Confidence: 0.55

- Wants public trust/social-proof metrics on marketing surfaces to never start at zero for a new business: explicitly asked to turn the "0+ booking sukses" hero counter into a hardcoded baseline (300 completed bookings, all rated 5★) dynamically merged with the real completed-booking count, and to give the empty reviews surface default testimonials plus the same 300×5★ baseline folded into the rating summary ("set the 0+ booking sukses to dynamicly add base line 300 booking sukses and for those 300 is 5 stars all... can we seed it or set default hardcode (will also add the dynamic one)"). The accepted shape: baseline numbers and default testimonials live as code constants merged at the service/summary layer (hero, landing section, and /reviews page all consume the same server-computed summary), not as seeded fake DB rows — so the DB stays real-data-only while the public site looks established and grows with live data. Fresh confirmation: after the first 3 defaults shipped, the user asked to expand the set ("add more dfault reviews"), expecting a substantive grid of believable testimonials (12 total) that spans every service category so the reviews surface and its filters look complete, not a minimal handful. Confidence: 0.7

- Treats the customer's Instagram handle as a first-class contact channel alongside phone/email for this business: asked for an optional Instagram username field to be added to every customer data-entry surface at once — the public "Kirimkan Pesan" contact form, the booking data-diri step, and the customer "My Profile" — expecting the same field collected consistently across all of them rather than on just one form. Fresh confirmation: also expects the handle to be surfaced in the backoffice, requesting it be shown in the messages inbox ("tampilkan instagram di @src/app/backoffice/messages"), where it renders as a clickable `instagram.com/<handle>` link in a new tab alongside phone/email. Confidence: 0.7

- Treats DOM/render cost of long scrolling lists as part of site performance, on par with image/network weight: explicitly requested scroll pagination that "only show image that visible in window" — expecting offscreen cards in long catalogs (e.g., the 181-item gallery) to be skipped for layout/paint/image-fetch (windowing) instead of accumulating every item in the DOM as the user scrolls. Confidence: 0.5

- Prefers low-friction guest flows over forcing account creation, even at the cost of a more complex data model: when offered "login required for booking" vs "guest + auto-claim on login + email signup", chose the guest path — keeping the booking funnel open for impulse visitors and resolving identity retroactively (claiming anonymous `user_id`-less customer rows by matching email/phone and attaching the `userId` on login) so past guest bookings surface under the account, rather than a hard login wall that taxes an impulse-purchase flow. Confidence: 0.6

- Prefers email over phone as the identity key for claiming/linking anonymous guest records to a logged-in account: when the claim logic matched by phone, questioned it directly ("hmm why not claimm based on email? not by phone") — phone numbers get recycled by carriers and shared between family members (a phone match can wrongly claim someone else's booking), while email is what signup/login is built on and is the stronger "same person" signal. The accepted design claims/merges by email only, on every profile fetch (not just first login), even at the cost that guests who book with only a phone number can't be auto-claimed. Confidence: 0.6

- Prefers identity linking for anonymous guest data to happen eagerly at the point of data creation rather than deferred to a later event: when the assistant claimed guest bookings at login time, questioned it directly ("hmm why need linked in login? why not when create booking then check is the customer/user with the email exist if yes then link") — the booking form always carries the customer's email regardless of auth state, so the accepted design resolves the customer inside `createBooking` (logged-in user → account row; anonymous guest whose email matches a registered account → link straight to that account at creation; anon row by phone as legacy fallback), demoting login-time claiming to a fallback safety net for what creation-linking can't catch (bookings with no email, legacy rows). Confidence: 0.6

- Requires email to be a mandatory field at booking creation, not optional — stated tersely as "email is required! when create bookin" right after the email-vs-phone linking discussion, making every booking carry a reliable identity key so creation-linking and login-claiming always have an email to match on (the phone lookup then becomes a legacy-only fallback). The change was applied to the shared customer-info schema (both the booking form and API) and the form copy, with the empty-string default kept only as react-hook-form's "unfilled" state. Confidence: 0.8
l, for backoffice history) vs `getActiveServices` (public/booking only), plus a kept-by-slug detail page with a "nonaktif" notice, was accepted as the right shape. Confidence: 0.9
service category so the reviews surface and its filters look complete, not a minimal handful. Confidence: 0.7
l, for backoffice history) vs `getActiveServices` (public/booking only), plus a kept-by-slug detail page with a "nonaktif" notice, was accepted as the right shape. Confidence: 0.9
